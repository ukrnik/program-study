DOM-элементам соответствуют HTML-теги, содержащие текстовые атрибуты.
Доступ к атрибутам осуществляется с помощью стандартных методов. Эти методы работают со значением, находящимся в HTML.

```html
<img class="image" src="https://picsum.photos/id/15/320/240" alt="Rocks and waterfall" width="300" />
```

 Элемент состоит из:

1. **Тега** (например: div, p, img).
2. **Атрибутов** (например: class, id, src).
3. **Содержимого** (текст или другие элементы внутри элемента).
---

<font color="#fcb97d">`ris:Question`</font> Получить доступ или изменить значения некоторых атрибутов элемента можно напрямую, обратившись к ним как к свойствам DOM-объекта. Это будет менее затратно по количеству кода.

---
## Основные методы Element:

| [[Метод element.hasAttribute(nameAttribute)]]<br>        | Проверяет наличие атрибута у елемента       |
| -------------------------------------------------------- | ------------------------------------------- |
| [[Метод element.getAttribute(nameAttribute)]]            | Возвращает текст из атрибута елемента       |
| [[Метод element.setAttribute(nameAttribute, value)]]<br> | Изменяет или добавляет атрибут со значением |
| [[Метод element.removeAttribute(nameAttribute)]]         | Удаляет атрибут из тега                     |
