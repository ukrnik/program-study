.pop() позволяет нам удалить последний элемент из масива

В скобки () мы ничего не передаем а значит метод удаляет только один раз из конца

```js
const courses = ['HTML', 'CSS', 'JavaScript'];
courses.pop()

console.log(courses); // ['HTML', 'CSS']
```
