## **Метод .forEach(callback)**

`forEach(callback)` — это метод перебора массива и коллекций, который выполняет указанную функцию **для каждого элемента**.  
Используется как замена циклов [[Цикл for]] и [[Цикл for...of]], когда не нужно прерывать выполнение.  

📌 Важно:  
- `forEach` **всегда перебирает всю коллекцию до конца** (его нельзя остановить с помощью `break` или `return`).  
- Метод **ничего не возвращает** (всегда `undefined`).  
- Подходит для действий «для каждого элемента» (логирование, модификации, вызовы функций).  

---

## 🔹 Синтаксис
```js
array.forEach(function callback(element, index, array) {
  // тело коллбек-функции
});
```
`
Аргументы коллбека:

- element → текущий элемент массива
- index → индекс текущего элемента
- array → сам массив, который перебирается

---
## **🔹 Примеры**
### **1. Перебор массива чисел**

```js
const numbers = [5, 10, 15, 20, 25];

numbers.forEach(function (number, index) {
  console.log(`Index ${index}, value ${number}`);
});
```

---
### **2. Массивы**

```js
let numbers = [1, 2, 3, 4, 5];

numbers.forEach(function(number) {
  console.log(number * 2); // умножаем каждый элемент на 2
});
```

---
### **3. Массив объектов**

```js
let users = [
  { name: 'Никита', age: 23 },
  { name: 'Настя', age: 22 },
  { name: 'Иван', age: 24 }
];

users.forEach(function(user) {
  console.log(`${user.name} - ${user.age} лет`);
});
```

```
Никита - 23 лет
Настя - 22 лет
Иван - 24 лет
```

---
### **4. Строки (String)**

```js
let str = 'Hello';

[...str].forEach(function(char) {
  console.log(char);
});
```

```
H
e
l
l
o
```

---
### **5. Map**

```js
let map = new Map([
  ['name', 'Никита'],
  ['age', 23],
  ['city', 'Киев']
]);

map.forEach(function(value, key) {
  console.log(`${key}: ${value}`);
});
```

```
name: Никита
age: 23
city: Киев
```

---
### **6. Set**

```js
let uniqueNumbers = new Set([1, 2, 3, 4, 4, 5]);

uniqueNumbers.forEach(function(number) {
  console.log(number);
});
```

```
1
2
3
4
5
```

---
### **7. NodeList (DOM)**

```html
<ul>
  <li class="item">Элемент 1</li>
  <li class="item">Элемент 2</li>
  <li class="item">Элемент 3</li>
</ul>
```

```js
let items = document.querySelectorAll('.item');

items.forEach(function(item) {
  console.log(item.textContent);
});
```

```
Элемент 1
Элемент 2
Элемент 3
```

---
## **⚖️ Отличие от других перебирающих методов**

- .forEach() → перебирает, **не возвращает результат** (только выполняет действие).
- .map() → перебирает и **возвращает новый массив**.
- .reduce() → сводит массив к одному значению.