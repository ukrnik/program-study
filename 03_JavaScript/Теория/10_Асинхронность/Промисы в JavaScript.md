# 📌 Промисы в JavaScript — просто и по делу

## 🔹 Что такое промис?

**Promise (промис)** — это специальный объект, который представляет **результат асинхронной операции**.

📦 Пример из жизни:  
> Полли пообещала испечь торт.  
> Если всё хорошо — получим торт.  
> Если нет — устроим праздник без торта.  
> Так и промисы: обещают результат, но не гарантируют.

### Промис может быть в 3 состояниях:
- `pending` — ожидание (по умолчанию)
- `fulfilled` — успешно выполнено
- `rejected` — произошла ошибка

---

## 🔹 Создание промиса

```js
const promise = new Promise((resolve, reject) => {
  // Асинхронный код
  if (успех) {
    resolve("Результат");
  } else {
    reject("Ошибка");
  }
});
```

✅ `resolve()` — вызывается при успехе  
❌ `reject()` — вызывается при ошибке

---

## 🔹 Обработка результата: `.then()` и `.catch()`

```js
promise
  .then(result => {
    console.log("Успех:", result);
  })
  .catch(error => {
    console.error("Ошибка:", error);
  });
```

- `.then()` — вызывается при `fulfilled`  
- `.catch()` — вызывается при `rejected`

---

## 🔹 Метод `.finally()` — выполнится всегда

```js
promise
  .then(data => console.log(data))
  .catch(err => console.log(err))
  .finally(() => console.log("Промис завершён"));
```

- Не получает аргументов
- Полезен для «уборки» после промиса

---

## 🔹 Цепочка промисов

```js
new Promise(resolve => resolve(2))
  .then(val => {
    console.log(val); // 2
    return val * 2;
  })
  .then(val => {
    console.log(val); // 4
    return val * 2;
  })
  .then(val => {
    console.log(val); // 8
  });
```

- Каждый `.then()` возвращает новый промис
- Ошибка в цепочке передаётся в ближайший `.catch()`

---

## 🧠 Важно:

- `.then()` всегда возвращает новый промис
- Если `resolve()` возвращает другой промис — `.then()` будет ждать его завершения
- Обработку ошибок можно делать через `.catch()` или вторым аргументом в `.then()`
- Состояние промиса нельзя изменить после `fulfilled` или `rejected`

---

## 🧪 Вопросы для самопроверки:

- ✅ Какие состояния бывают у промиса?  
  → `pending`, `fulfilled`, `rejected`
- ✅ Что делает `.then()`?  
  → Обрабатывает успешный результат
- ✅ Что делает `.catch()`?  
  → Ловит ошибку
- ✅ Что возвращает `.then()`?  
  → Новый промис
- ✅ Когда вызывается `.finally()`?  
  → Всегда, после завершения промиса

---

## ✅ Итог

Промисы позволяют писать асинхронный код так, словно он синхронный:
- Управляют результатом и ошибками
- Позволяют строить цепочки асинхронных действий
- Применяются в работе с API, таймерами, сетевыми запросами

🚀 Основы промисов необходимы для комфортной работы с `fetch`, `async/await` и построения надёжного кода.