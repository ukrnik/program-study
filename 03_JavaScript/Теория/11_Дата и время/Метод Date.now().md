## **Метод Date.now()**

`Date.now()` — это **статический метод** объекта `Date`, который возвращает количество миллисекунд, прошедших с **1 января 1970 года 00:00:00 UTC** (Unix Epoch).  

---

## 📌 Особенности
- Не требует создания экземпляра `Date`.  
- Работает быстрее, чем `new Date().getTime()`, так как не создаёт объект.  
- Возвращает число (тип `number`).  

---

## 🔹 Синтаксис
```js
Date.now();
```

> [!info]
> Возвращает: Number — количество миллисекунд с 01.01.1970 UTC.

---
## **🔹 Пример использования**

```js
console.log(Date.now()); 
// Например: 1724421134567
```

---
## **🔹 Замер времени выполнения кода**

Метод удобно использовать для измерения времени выполнения операций:

```js
const start = Date.now();

// Код, время выполнения которого нужно замерить
for (let i = 0; i < 1e6; i++) {}

const end = Date.now();
console.log(`Время выполнения: ${end - start} мс`);
```
