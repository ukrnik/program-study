## Описание

В JavaScript для работы с датами и временем используется встроенный объект `Date`.

```js
const now = new Date();
console.log(now); // текущая дата и время 
// (Fri Apr 04 2025 17:09:48 GMT+0200 (Центральная Европа, летнее время))
```

> [!done]
> Создает объект с текущей датой и временем.

---
## 🔹 Создание объектов Date

### 🔸 Текущий момент:

```js
const now = new Date();
```

### 🔸 Из строки:

```js
console.log(new Date("2030")); 
// "Tue Jan 01 2030 02:00:00 GMT+0200"

console.log(new Date("2030-03")); 
// "Fri Mar 01 2030 02:00:00 GMT+0200"

console.log(new Date("2030-03-16")); 
// "Sat Mar 16 2030 02:00:00 GMT+0200"

console.log(new Date("2030-03-16T14:25:00")); 
// "Sat Mar 16 2030 14:25:00 GMT+0200"
```

### 🔸 Из отдельных значений:

```js
const specificDate = new Date(2025, 3, 4, 10, 30); 
// 4 апреля 2025 года, 10:30
```

> [!attention]
> Месяцы начинаются с **0** (январь = 0, декабрь = 11)

---
## 🔹 Unix время

> [!info]
> Для компьютеров отсчет времени считается в миллисекундах, прошедших после полуночи 1 января 1970 года в часовом поясе UTC (Coordinated Universal Time). Это - Unix время.

```js
console.log(new Date(0));
// "Thu Jan 01 1970 03:00:00 GMT+0300 (Eastern European Standard Time)"

console.log(new Date(15000));
// "Thu Jan 01 1970 03:00:15 GMT+0300 (Eastern European Standard Time)"
```

> [!question]
> Встроенный метод getTime() возвращает числовое значение этой даты (timestamp) - количество миллисекунд, прошедшее с полуночи 1 января 1970 года.

```js
const date = new Date();
console.log(date.getTime()); // 1624021654154
```

---
## **Метод** Date.now()

Мы уже рассматривали пример создания объекта времени. У него есть метод getTime, который позволяет получить числовое представление момента времени в количестве миллисекунд.

```js
const date = new Date();
console.log(date.getTime()); // 1624021654154
```

В случае, когда все, что нужно, это числовое представление момента времени, нет смысла создавать целый объект. Можно использовать метод Date.now() - это быстрый и удобный способ получить текущее время в виде миллисекунд

```js
const time = Date.now(); // 1693237207904
```

Основная идея применения Date.now() заключается в измерении времени, расчете интервалов и тому подобное.

### Пример: Измерение времени выполнения кода

Если ты хочешь измерить, сколько времени занимает выполнение некоторого кода, можно сделать так:

```js
const startTime = Date.now();

// Твій код, виконуваний упродовж деякого часу
for(let i = 0; i <= 100; i += 1) {
	console.log(i);
}

const endTime = Date.now();
const elapsedTime = endTime - startTime;

console.log(`Elapsed time: ${elapsedTime} ms`);
```

> [!quotes]
> - Зберегти час до події.
> - Зберегти час після події.
> - Порахувати різницю між кінцевим і початковим часом.

---
## 🔹 Геттеры и сеттеры

Экземпляр класса Date имеет множество методов для чтения и записи значений даты и времени.

### 🔸 Получение компонентов даты

Геттеры используются для чтения всей даты или отдельной составляющей. Возвращаемое значение зависит от текущего часового пояса, установленного на компьютере.

```js
const date = new Date();

console.log(date.getFullYear()); // год
console.log(date.getMonth());    // месяц (0–11)
console.log(date.getDate());     // день месяца (1–31)
console.log(date.getDay());      // день недели (0 – воскресенье, 6 – суббота)
console.log(date.getHours());    // часы
console.log(date.getMinutes());  // минуты
console.log(date.getSeconds());  // секунды
```

---
### 🔸 Установка компонентов даты

```js
const date = new Date();

date.setFullYear(2030);
date.setMonth(0);     // январь
date.setDate(15);
```

> [!done]
> Обновляет значения даты.

---
## 🔹 Форматирование даты

### 🔸 Простой способ (методы):

```js
const date = new Date();

console.log(date.toDateString()); // "Fri Apr 04 2025"
console.log(date.toTimeString()); // "10:21:08 GMT+0200"
console.log(date.toISOString());  // "2025-04-04T08:21:08.123Z"
```

### 🔸 Локализованный формат:

```js
const date = new Date();

const options = {
  weekday: "long",
  year: "numeric",
  month: "long",
  day: "numeric"
};

console.log(date.toLocaleDateString("ru-RU", options));
// пример: "пятница, 4 апреля 2025 г."
```

---
## 🔹 Разница между датами

```js
const start = new Date("2025-04-01");
const end = new Date("2025-04-04");

const diffMs = end - start; // разница в миллисекундах
const diffDays = diffMs / (1000 * 60 * 60 * 24);

console.log(diffDays); // 3
```

> [!done]
> Можно вычитать даты, чтобы узнать разницу в днях, часах и т.д.
---
## 🔹 Таймеры: setTimeout и setInterval
### 🔸 setTimeout – выполнить 1 раз с задержкой

```js
setTimeout(() => {
  console.log("Прошло 2 секунды");
}, 2000);
```
### 🔸 setInterval – выполнять регулярно

```js
let count = 0;
const timer = setInterval(() => {
  count++;
  console.log(`Прошло ${count} секунд`);

  if (count === 5) clearInterval(timer);
}, 1000);
```

---