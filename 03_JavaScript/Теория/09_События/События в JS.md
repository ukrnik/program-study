## Описание

Событие – это сигнал от браузера о том, что на веб-странице что-то произошло. Существует множество видов событий: события мыши, события клавиатуры, события элементов форм, изменения размеров окна, загрузка изображений, буфер обмена, изменения стадии CSS анимации или перехода и т.д. События используются для реакции на действия пользователя и выполнения кода, связанного с определенным событием.

Чтобы элемент реагировал на действия пользователя, к нему необходимо добавить слушателя события и определить ему обработчика.

Слушатель события - это механизм, который "слушает" или "ожидает" возникновения определенного события. [[Метод addEventListener()]] добавляет события слушателя на элемент.

---
## Методы для роботы с событиями:

| [[Метод addEventListener()]]    | Добавляет обработчик событий |
| ------------------------------- | ---------------------------- |
| [[Метод removeEventListener()]] | Удаляет обработчик событий   |
| [[События клавиатуры]]          | Событие с клавишами          |
| [[Свойство key і code]]         | Информация о клавишах        |

## События элементов форм:

| [[Событие submit]]       |     |
| ------------------------ | --- |
| [[Событие change]]       |     |
| [[Событие input]]        |     |
| [[Событие focus и blur]] |     |

---
## Объект события

### Описание

Объект события (event object) — это специальный объект, который содержит информацию о том, что произошло (например, клик мыши, нажатие клавиши, прокрутка страницы и т.д.).

Когда происходит событие (например, нажали на кнопку), браузер автоматически создаёт объект события и передаёт его в обработчик.

---
### Как работает объект события?

Когда мы добавляем обработчик события, можно получить объект события, передав его в функцию.

```js
document.querySelector("button").addEventListener("click", function(event) {
    console.log(event);  // Выведет объект события в консоль
});f
```

- Здесь event — это объект события.
- В нём содержится информация, например, координаты клика, какая кнопка мыши была нажата, какие клавиши зажаты и т.д.

---
### Какие свойства есть у объекта события?

| Свойство         | Что делает?                                                                   |
| ---------------- | ----------------------------------------------------------------------------- |
| type             | Тип события (click, keydown, scroll и т.д.)                                   |
| target           | Элемент, на котором произошло событие                                         |
| clientX, clientY | Координаты клика                                                              |
| key              | Какая клавиша нажата (для keydown)                                            |
| preventDefault() | Отключает стандартное действие браузера (например, отмена перехода по ссылке) |

---
### Пример №1

```js
document.addEventListener("keydown", function(event) {
    console.log("Вы нажали клавишу: " + event.key);
});
```

<font color="#fcb97d">`ris:Question`</font> В консоль выведется текст ("Вы нажали клавишу: " и клавиша которую нажал пользователь)

---
### Пример №2

```html
<button type="button" class="btn">Click me</button>
```

```js
const button = document.querySelector(".btn");

const handleClick = (event) => {
  console.log("event: ", event);
  console.log("event type: ", event.type);
  console.log("currentTarget: ", event.currentTarget);
};

button.addEventListener("click", handleClick);
```

`fas:ArrowDown` Выведет в консоль тип события, элемент, на котором выполняется обработчик происшествия..

```
"event: " // [object PointerEvent] 
{
  "isTrusted": …
}

---------------------------------------------------------------------

"event type: " "click"

---------------------------------------------------------------------

"currentTarget: " <button type="button" class="btn">Click me</button>
```

---
### ИТОГ:

📌 **Объект события** — это штука, которая содержит всю информацию о том, что произошло. Он автоматически передаётся в обработчик событий. Мы можем:

 - Узнать, какой элемент был нажат (target)
 - Посмотреть координаты клика (clientX, clientY)
 - Определить нажатую клавишу (key)
 - Остановить стандартное действие (preventDefault())