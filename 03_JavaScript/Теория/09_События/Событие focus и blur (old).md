## Описание

События focus и blur используются для отслеживания, когда элемент получает или теряет фокус ввода (обычно это `input`, `textarea`, `select`).

| Событие | Когда срабатывает?                                                                        |
| ------- | ----------------------------------------------------------------------------------------- |
| focus   | Когда элемент получает фокус (например, пользователь кликнул в поле ввода или нажал Tab). |
| blur    | Когда элемент теряет фокус (например, пользователь нажал Tab, кликнул в другое место).    |

<font color="#409CFF">`fas:InfoCircle`</font> Проще говоря:

- focus = “Я начал вводить данные”
- blur = “Я закончил вводить данные”

---
## Пример №1: Отслеживаем фокус и потерю фокуса

```html
<input type="text" id="name" placeholder="Введите имя">
<p id="message"></p>
```

```js
let input = document.querySelector("#name");

input.addEventListener("focus", function() {
    document.querySelector("#message").textContent = "⌨ Начните вводить ваше имя...";
});

input.addEventListener("blur", function() {
    document.querySelector("#message").textContent = "✅ Ввод завершён.";
});
```

<font color="#fcb97d">`ris:Question`</font> Как это работает?

- Когда пользователь кликает в поле, появляется "⌨ Начните вводить ваше имя...".
- Когда он уходит из поля (blur), текст меняется на "✅ Ввод завершён.".

---
## Пример №2: Проверяем, пустое ли поле при потере фокуса

```html
<input type="text" id="email" placeholder="Введите email">
<p id="error-message"></p>
```

```js
document.querySelector("#email").addEventListener("blur", function(event) {
    if (event.target.value.trim() === "") {
        document.querySelector("#error-message").textContent = "❌ Поле не может быть пустым!";
    } else {
        document.querySelector("#error-message").textContent = "";
    }
});
```

<font color="#fcb97d">`ris:Question`</font> Как это работает?

- Если поле пустое и пользователь уходит (blur), показываем "❌ Поле не может быть пустым!".
- Если есть текст, скрываем сообщение.

---
## Пример №3: Добавление/удаление стилей при фокусе

<font color="#409CFF">`fas:InfoCircle`</font> Подсвечиваем поле при фокусе

```html
<input type="text" id="username" placeholder="Введите имя">
```

```css
  input:focus {
    border: 2px solid blue;
    outline: none;
  }
```

```js
let input = document.querySelector("#username");

input.addEventListener("focus", function() {
    input.style.border = "2px solid blue";
});

input.addEventListener("blur", function() {
    input.style.border = "1px solid gray";
});
```

<font color="#fcb97d">`ris:Question`</font> Как это работает?

- Когда поле в фокусе, оно меняет границу на синюю.
- Можно сделать это через JavaScript:

---
## Пример №4: Автоматический фокус на поле при загрузке

```html
<input type="text" id="firstInput" placeholder="Сначала заполни меня!">
```

```js
document.querySelector("#firstInput").focus(); // Автофокус на поле
```

<font color="#fcb97d">`ris:Question`</font> Как это работает?

- Когда страница загружается, курсор сразу ставится в первое поле.

---
## Пример №5: Фокус и blur для нескольких элементов (делегирование)

<font color="#409CFF">`fas:InfoCircle`</font> Отслеживаем фокус и blur у всех input в form

```html
<form id="myForm">
    <input type="text" name="name" placeholder="Имя">
    <input type="email" name="email" placeholder="Email">
    <input type="password" name="password" placeholder="Пароль">
</form>
```

```js
document.querySelector("#myForm").addEventListener("focus", function(event) {
    if (event.target.tagName === "INPUT") {
        event.target.style.backgroundColor = "lightyellow";
    }
}, true); // Важно: третьим аргументом передаём `true` (используем `capture`)

document.querySelector("#myForm").addEventListener("blur", function(event) {
    if (event.target.tagName === "INPUT") {
        event.target.style.backgroundColor = "white";
    }
}, true);
```

<font color="#fcb97d">`ris:Question`</font> Как это работает?

- Когда любое поле получает focus, его фон меняется на жёлтый.
- Когда оно теряет blur, фон становится белым.
- Используем true в addEventListener(), чтобы событие срабатывало при всплытии.

---
## Пример №6: Убираем фокус после клика по кнопке

```html
<input type="text" id="search" placeholder="Поиск...">
<button id="clear">Очистить</button>
```

```js
document.querySelector("#clear").addEventListener("click", function() {
    let input = document.querySelector("#search");
    input.value = ""; // Очищаем поле
    input.blur(); // Убираем фокус
});
```

<font color="#fcb97d">`ris:Question`</font> Как это работает?

- input.blur() убирает фокус после клика по кнопке.
- Полезно, когда не хочется, чтобы курсор оставался в поле после очистки.

---
## ИТОГ:

📌 `focus` срабатывает, когда элемент получает фокус (курсор в поле).
📌 `blur` срабатывает, когда элемент теряет фокус (переход на другой элемент).
📌 Используются для валидации, стилизации, автофокуса.
📌 Можно программно ставить и убирать фокус через `.focus()` и `.blur().`
