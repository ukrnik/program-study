## **Функция Анонимная**

**Анонимная функция** — это функция **без имени**.  
Она создаётся «на месте» и часто используется как **одноразовая** или как [[Функция Callback]]

---

## 📌 Особенности
- Не имеет имени (идентификатора).  
- Обычно используется **инлайн** (внутри вызова другой функции).  
- Можно присвоить переменной и использовать как обычную функцию.  
- Удобна для **разовых действий** или **передачи в качестве аргумента**.  
- Часто записывается через стрелочный синтаксис `()=>{}`.  

---

## 🔹 Синтаксис

### 1. Обычная анонимная функция
```js
setTimeout(function() {
  console.log("Привет из анонимной функции!");
}, 1000);
```

### **2. Анонимная функция в переменной**

```js
const sayHi = function() {
  console.log("Привет!");
};

sayHi(); // Привет!
```

### **3. Стрелочная анонимная функция**

```js
setTimeout(() => {
  console.log("Привет из стрелочной анонимной функции!");
}, 1000);
```

---
## **🔹 Примеры использования**
### **1. Как callback**

```js
[1, 2, 3].forEach(function(num) {
  console.log(num * 2);
});
```

### **2. В обработчике событий**

```js
document.querySelector("#btn").addEventListener("click", function() {
  console.log("Кнопка нажата!");
});
```

---
## **⚖️ Отличие от именованных функций**

```js
// Именованная функция
function greet() {
  console.log("Привет!");
}

// Анонимная функция
const greet2 = function() {
  console.log("Привет!");
};
```


- **Именованная функция** → имеет имя, можно вызывать в любом месте (поддерживает hoisting).
- **Анонимная функция** → имени нет, её можно использовать только через переменную или напрямую как callback.

---
## **📌 Итог**

Анонимные функции — это **одноразовые функции без имени**, которые часто используют как **инлайн-коллбеки** в массивах, событиях и асинхронных вызовах.