## Описание

**Деструктуризация массива** – это способ **извлечения элементов массива в отдельные переменные**.

📌 **Простыми словами:**

Вместо того чтобы обращаться к элементам массива через индексы `(arr[0], arr[1])`, можно **разложить** их в переменные.

---
## Синтаксис

**Простая деструктуризация массива**

```js
const numbers = [1, 2, 3];

const [a, b, c] = numbers;

console.log(a); // 1
console.log(b); // 2
console.log(c); // 3
```

✅ Теперь a = 1, b = 2, c = 3.

---
## Пропуск элементов массива

Можно **пропустить ненужные элементы**, оставив пустые места.

```js
const numbers = [10, 20, 30, 40];

const [, second, , fourth] = numbers;

console.log(second); // 20
console.log(fourth); // 40
```

✅ Мы пропустили 10 и 30, записав только 20 и 40.

---
## Значения по умолчанию

Если элемент отсутствует, можно задать **значение по умолчанию**.

```js
const numbers = [5];

const [a = 10, b = 20] = numbers;

console.log(a); // 5 (из массива)
console.log(b); // 20 (значение по умолчанию)
```

✅ b получает **20**, потому что второго элемента в массиве нет.

---
## Деструктуризация с rest оператором (...)

Можно **собирать остаток массива** в переменную.

```js
const numbers = [1, 2, 3, 4, 5];

const [first, second, ...rest] = numbers;

console.log(first);  // 1
console.log(second); // 2
console.log(rest);   // [3, 4, 5]
```

✅ rest получает **все оставшиеся элементы** массива.

---
## Деструктуризация в функциях

Можно использовать **деструктуризацию в аргументах функции**.

```js
function getCoordinates() {
    return [50.123, 30.456];
}

const [latitude, longitude] = getCoordinates();

console.log(`Latitude: ${latitude}, Longitude: ${longitude}`);
```

✅ Функция возвращает массив, и его элементы **сразу записываются в переменные**.

---
## Деструктуризация вложенных массивов

Можно **разбирать вложенные массивы**.

```js
const matrix = [
    [1, 2],
    [3, 4],
];

const [[a, b], [c, d]] = matrix;

console.log(a, b, c, d); // 1 2 3 4
```

✅ Теперь a = 1, b = 2, c = 3, d = 4.

---
## Обмен значений переменных через деструктуризацию

Обычно для обмена значений используется временная переменная:

```js
let a = 1, b = 2;
let temp = a;
a = b;
b = temp;

console.log(a, b); // 2, 1
```

Но с **деструктуризацией** можно **обменять переменные без временной переменной**:

```js
let a = 1, b = 2;

[a, b] = [b, a];

console.log(a, b); // 2, 1
```

✅ Коротко и просто!

---
## ИТОГ:

 ✔ **Деструктуризация массива** упрощает доступ к его элементам.
✔ **Можно пропускать элементы, задавать значения по умолчанию и использовать rest-оператор**.
✔ **Работает с вложенными массивами и функциями, которые возвращают массивы**.
✔ **Можно использовать для обмена значений переменных без временной переменной**.
