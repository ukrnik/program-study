## Описание

**Веб-хранилища (Web Storage)** – это технология, позволяющая браузерам сохранять данные локально на стороне клиента. В отличие от cookies, веб-хранилища не отправляют данные на сервер при каждом запросе.

📌 **Простыми словами:**

Это встроенная в браузер база данных, где можно **хранить данные прямо в браузере**, даже после закрытия вкладки.

---
##  **Виды веб-хранилищ**

В браузере есть **два типа хранилищ**:

| **Хранилище**  | **Когда очищается?**          | **Доступ к данным**                 |
| -------------- | ----------------------------- | ----------------------------------- |
| localStorage   | Никогда (до ручного удаления) | Доступен на всех страницах домена   |
| sessionStorage | После закрытия вкладки        | Доступен только на текущей странице |

**🔸 Когда использовать?**

✔ [[Постоянное хранилище localStorage|localStorage]] – для **долгосрочного хранения** (настройки, сохраненные данные).

✔ [[Временное хранилище SessionStorage|sessionStorage]] – для **временных данных**, которые нужны только во время сессии (например, статус модального окна).

✔ [[Файлы Cookies|Cookies]] 🍪 – если **данные должны отправляться на сервер** (авторизация, сессии, языковые настройки).