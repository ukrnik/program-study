## 📑 Оглавление

| [[#Promise (Промис) — асинхронные операции в JavaScript]] |     |
| --------------------------------------------------------- | --- |
| [[#Promise.resolve() и Promise.reject()]]                 |     |
| [[#Promise.all()]]                                        |     |
| [[#Метод Promise.allSettled()]]                           |     |
| [[#Метод Promise.race()]]                                 |     |

----
## Promise (Промис) — асинхронные операции в JavaScript

`Promise` — это объект, который представляет результат асинхронной операции.  
Он может находиться в одном из трёх состояний:

- **pending** — ожидание (операция выполняется)  
- **fulfilled** — успешно выполнено (результат получен)  
- **rejected** — выполнено с ошибкой  

> [!scale]
> Полі обіцяє спекти торт 🎂 на день народження:  
> - ✅ Якщо здорова → торт є (**resolve**)  
> - ❌ Якщо захворіла → торта немає (**reject**)  
> - 🎉 Але вечірка буде в будь-якому випадку (**finally**)  
>
>👉 Promise = обіцянка результату в майбутньому (успіх або помилка).

---
## **Создание Promise**

Promise создаётся как экземпляр класса `Promise`, который принимает **функцию-исполнитель (executor)**.  

Эта функция вызывается **сразу при создании промиса** и имеет два параметра:  

- `resolve(value)` — вызывается при успешном завершении, переводит промис в состояние **fulfilled**  
- `reject(error)` — вызывается при ошибке, переводит промис в состояние **rejected**  

  Значение, возвращаемое функцией-исполнителем, игнорируется.  
Важно вызывать `resolve()` или `reject()` в нужный момент.

```js
const promise = new Promise((resolve, reject) => {
  // Асинхронная операция
  setTimeout(() => {
    const success = true;

    if (success) {
      resolve("Операция выполнена успешно!");
    } else {
      reject("Произошла ошибка!");
    }
  }, 2000);
});
// Сначала: Promise { <pending> }
// Через 2с: Promise { "✅ Успех!" } или Promise { "❌ Ошибка!" }
```

---
## **Методы промиса**

|**Метод**|**Описание**|**Пример**|
|---|---|---|
|.then()|Выполняется при успешном завершении (fulfilled)|promise.then(result => console.log(result))|
|.catch()|Выполняется при ошибке (rejected)|promise.catch(error => console.error(error))|
|.finally()|Выполняется всегда, независимо от результата|promise.finally(() => console.log("Завершено"))|

---
## **Пример использования**

![[Promise.png]]

```js
promise
  .then(result => {
    console.log("✅ Успех:", result);
  })
  .catch(error => {
    console.error("❌ Ошибка:", error);
  })
  .finally(() => {
    console.log("🔔 Операция завершена");
  });
```

---
## **Цепочка промисов**

Промисы можно связывать в цепочку, чтобы выполнять операции последовательно.

![[Pasted image 20250823032249.png]]

```js
const promise = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve(5);
  }, 2000);
});

promise
  .then(value => {
    console.log(value); // 5
    return value * 2;
  })
  .then(value => {
    console.log(value); // 10
    return value * 3;
  })
  .then(value => {
    console.log(value); // 30
  })
  .catch(error => {
    console.log(error);
  })
  .finally(() => {
    console.log("finally");
  });
```

> [!error]
> Метод then() возвращает промис. Перед его выполнением может пройти некоторое время, поэтому оставшаяся часть цепочки будет ждать результат. В случае возникновения ошибки в любом месте цепочки выполнение всех последующих then() отменяется, а управление передаётся методу catch(). Поэтому он должен находиться в конце всей цепочки then().

```js
Promise.resolve(5)
  .then(x => x * 2)              // 10
  .then(x => { throw "Помилка"; }) // ❌
  .then(x => console.log(x))     // пропускається
  .catch(err => console.error(err)); // "Помилка"
```



---
## **Статические методы Promise**

|**Метод**|**Описание**|**Пример**|
|---|---|---|
|Promise.resolve(value)|Создаёт промис в состоянии fulfilled со значением value|Promise.resolve(42)|
|Promise.reject(error)|Создаёт промис в состоянии rejected с ошибкой error|Promise.reject("Ошибка")|
|Promise.all([...])|Ждёт выполнения всех промисов, возвращает массив результатов|Promise.all([p1, p2])|
|Promise.race([...])|Возвращает результат первого завершившегося промиса|Promise.race([p1, p2])|
|Promise.allSettled([...])|Ждёт выполнения всех промисов, возвращает статусы каждого|Promise.allSettled([p1, p2])|
|Promise.any([...])|Возвращает первый **успешный** промис, игнорируя ошибки|Promise.any([p1, p2])|

---
### Promise.resolve() и Promise.reject()

Эти методы позволяют быстро создавать промисы в состоянии **успеха** или **ошибки**, без явного использования new Promise(...).

- **Promise.resolve(value)** → возвращает успешно выполненный промис с результатом value.
- **Promise.reject(reason)** → возвращает отклонённый промис с причиной ошибки reason.

Стандартная запись через  new Promise(...)

```js
const makeGreeting = guestName => {
	return new Promise((resolve, reject) => {
		  if (!guestName) {
				reject("Guest name must not be empty");
		  } else {
				resolve(`Welcome ${guestName}`);
			}
	})
};

makeGreeting("Mango")
  .then(greeting => console.log(greeting))
  .catch(error => console.error(error));
```

После [[Асинхронность - Промисификация]]:

```js
const makeGreeting = guestName => {
  if (!guestName) {
   return Promise.reject("Guest name must not be empty");
  } else {
		return Promise.resolve(`Welcome ${guestName}`);
	}
};

makeGreeting("Mango")
  .then(greeting => console.log(greeting))
  .catch(error => console.error(error));
```


---
### Promise.all()

> [!info]
> Метод Promise.all() позволяет одновременно обработать несколько промисов и получить их результаты. Он принимает массив промисов в качестве входных данных, ожидает их выполнения и возвращает один промис.

```js
Promise.all([promise1, promise2, promise3, ...])
```

Если все промисы выполняются успешно, возвращенный промис перейдет в состояние fulfilled, а его значением будет массив результатов выполнения каждого промиса.

```js
const p1 = Promise.resolve(1);
const p2 = Promise.resolve(2);
const p3 = Promise.resolve(3);

Promise.all([p1, p2, p3])
	.then(values => console.log(values)) // [1, 2, 3]
	.catch(error => console.log(error));
```

Это означает, что он ждет выполнения всех промисов, а затем создает и возвращает новый промис, значением которого является массив значений выполненных промисов.

![[9085149d-cbb3-4432-b194-fa0e2cb7477eБез імені.jpg]]

Если хотя бы один из промисов отклоняется, возвращенный промис сразу переходит в состояние rejected, а его значением будет ошибка.

```js
const p1 = Promise.resolve(1);
const p2 = Promise.reject("Rejected promise 2");
const p3 = Promise.resolve(3);

Promise.all([p1, p2, p3])
	.then(values => console.log(values))
	.catch(error => console.log(error)); // "Rejected promise 2"
```

![[29c19789-edf2-408b-9ef6-5180ca5847b31111111111111.jpg]]

---
### Метод Promise.allSettled()

Метод Promise.allSettled() также позволяет одновременно обработать несколько промисов и получить их результаты. Он принимает массив промисов в качестве входных данных, ожидает их выполнения и возвращает один промис.

```js
Promise.allSettled([promise1, promise2, promise3, ...])
```

Отличие от Promise.all заключается в том, что метод Promise.allSettled() ожидает выполнения всех промисов независимо от того, были ли некоторые или даже все промисы отклонены.

![[4162e72a-f304-4f48-b7f4-32c472345225222222.jpg]]

Возвращенный промис никогда не будет отклонен, он всегда успешно выполняется (состояние fulfilled). Добавление метода catch даже не имеет смысла, поскольку он никогда не будет выполнен.

```js
const p1 = Promise.resolve(1);
const p2 = Promise.reject("Rejected promise 2");
const p3 = Promise.resolve(3);

Promise.allSettled([p1, p2, p3])
	.then(values => console.log(values));
	// [
	//   { status: "fulfilled", value: 1}, 
  //   { status: "rejected", value: "Rejected promise 2"},
  //   { status: "fulfilled", value: 3}
  // ]
```

Значением возвращаемого промиса будет массив объектов с результатами выполнения каждого промиса.

- status — свойство, которое хранит статус, с которым выполнился промис, это строка «fulfilled» или «rejected».
- value — свойство, в котором хранится значение, с которым выполнился или был отклонен промис.

---
### Метод Promise.race()

Метод Promise.race принимает массив промисов и возвращает «самый быстрый», то есть первый выполненный или отклоненный промис из переданных, вместе со значением или причиной его отклонения.

```js
Promise.race([promise1, promise2, promise3, ...])
```

#### Кейс 1
Создадим несколько промисов с разным временем выполнения.

```js
const p1 = new Promise((resolve, reject) => {
  setTimeout(() => resolve(1), 1000);
});

const p2 = new Promise((resolve, reject) => {
  setTimeout(() => reject(2), 2000);
});

Promise.race([p1, p2])
	.then(value => console.log(value)); // 1
	.catch(error => console.log(error));
```

Первый промис перейдет в состояние fulfilled через 1 секунду (будет самым быстрым), и будет выполнен колбек метода then со значением первого промиса, а остальные будут отклонены.

Когда хотя бы один промис из массива выполнится, возвращаемый промис перейдет в состояние resolved, а все остальные будут отклонены.

![[dbc4b23f-6c6b-417d-a263-6727d0c3dc23Screenshot 2023-08-28 at 17.05.10.png]]

---
#### Кейс 2
Создадим несколько промисов с разным временем выполнения.

```js
const p1 = new Promise((resolve, reject) => {
  setTimeout(() => resolve(1), 2000);
});

const p2 = new Promise((resolve, reject) => {
  setTimeout(() => rejected(2), 1000);
});

Promise.race([p1, p2])
	.then(value => console.log(value)); 
	.catch(error => console.log(error)); // 2
```

Второй промис перейдет в состояние rejected через 1 секунду (будет самым быстрым), выполнится колбек метода catch со значением второго промиса, а остальные будут отклонены.

Когда хотя бы один промис из массива будет отклонен, возвращаемый промис перейдет в состояние rejected, а все остальные будут отклонены.

![[b3ecad4f-2c47-4cbe-8d3d-f622f64580b5Screenshot 2023-08-28 at 17.10.04.png]]

---
## **Итого**

- Promise упрощает работу с асинхронным кодом (замена вложенных коллбеков).
- Основные методы: .then(), .catch(), .finally().
- Можно объединять несколько промисов через Promise.all, Promise.race и др.
- Для упрощения работы используется синтаксис **async/await** (см. [[Асинхронность - async await]]).