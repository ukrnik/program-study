## **Объект Date — работа с датой и временем**

Date — встроенный объект JavaScript для работы с датами и временем. Позволяет получать текущую дату, время, а также изменять их.

---
## **Создание объекта Date**

```js
// Текущая дата и время (по локальному времени компьютера)
const now = new Date();
console.log(now); // "Fri Jun 18 2021 15:01:35 GMT+0300 (Eastern European Summer Time)"

// Из миллисекунд с 1 января 1970 (UNIX-время)
const fromTimestamp = new Date(1700000000000);

// Из строки даты (ISO-формат)
const fromString = new Date('2025-08-15T10:30:00');

// Год, месяц (0–11), день, часы, минуты, секунды, миллисекунды
const customDate = new Date(2025, 7, 15, 10, 30, 0);
```

```js
console.log(new Date("2030")); // "Tue Jan 01 2030 02:00:00 GMT+0200"

console.log(new Date("2030-03")); // "Fri Mar 01 2030 02:00:00 GMT+0200"
console.log(new Date("2030-03-16")); // "Sat Mar 16 2030 02:00:00 GMT+0200"
console.log(new Date("2030-03-16T14:25:00")); // "Sat Mar 16 2030 14:25:00 GMT+0200"
```

> [!info]
>- Месяцы считаются с **0** (январь) до **11** (декабрь).
>- Строки в формате **ISO (YYYY-MM-DDTHH:mm:ss)** интерпретируются как время в UTC, если не указано смещение.
>- new Date(ms) использует миллисекунды с 1970-01-01T00:00:00Z (UTC).

---
## Методы получения даты и времени

| Метод                | Описание                                         | Пример результата |
| -------------------- | ------------------------------------------------ | ----------------- |
| `.getFullYear()`     | Возвращает год                                   | `2025`            |
| `.getMonth()`        | Возвращает месяц (0–11)                          | `7` (август)      |
| `.getDate()`         | Возвращает число месяца (1–31)                   | `15`              |
| `.getDay()`          | Возвращает день недели (0–6, 0 — воскресенье)    | `5` (пятница)     |
| `.getHours()`        | Возвращает часы (0–23)                           | `10`              |
| `.getMinutes()`      | Возвращает минуты (0–59)                         | `30`              |
| `.getSeconds()`      | Возвращает секунды (0–59)                        | `0`               |
| `.getMilliseconds()` | Возвращает миллисекунды (0–999)                  | `0`               |
| `.getTime()`         | Возвращает метку времени в миллисекундах         | `1755250200000`   |
| `Date.now()`         | Возвращает текущую метку времени в миллисекундах | `1755250200000`   |

---
## Методы установки даты и времени

| Метод               | Описание                                     | Пример |
| ------------------- | -------------------------------------------- | ------- |
| `.setFullYear(year)`| Устанавливает год                            | `date.setFullYear(2024)` |
| `.setMonth(month)`  | Устанавливает месяц (0–11)                    | `date.setMonth(0)` (январь) |
| `.setDate(day)`     | Устанавливает число месяца (1–31)             | `date.setDate(25)` |
| `.setHours(hours)`  | Устанавливает часы (0–23)                     | `date.setHours(15)` |
| `.setMinutes(min)`  | Устанавливает минуты (0–59)                   | `date.setMinutes(45)` |
| `.setSeconds(sec)`  | Устанавливает секунды (0–59)                  | `date.setSeconds(30)` |
| `.setMilliseconds(ms)`| Устанавливает миллисекунды (0–999)          | `date.setMilliseconds(500)` |
| `.setTime(ms)`      | Устанавливает дату по метке времени (мс)      | `date.setTime(1755250200000)` |

---
## **Форматирование даты**

```js
const date = new Date(2025, 7, 15, 10, 30);

console.log(date.toString());           // Fri Aug 15 2025 10:30:00 GMT+0200
console.log(date.toDateString());       // Fri Aug 15 2025
console.log(date.toTimeString());       // 10:30:00 GMT+0200
console.log(date.toLocaleDateString()); // 15.08.2025 (локальный формат)
console.log(date.toLocaleTimeString()); // 10:30:00 (локальный формат)
console.log(date.toISOString());        // 2025-08-15T08:30:00.000Z
```

> [!tip]
> toISOString() всегда возвращает дату/время в **UTC**.
> Для локального отображения используй .toLocaleDateString() и .toLocaleTimeString().

---
## **Пример использования**

```js
// Сколько ms выполнялся код программы
const startTime = Date.now();

// Твій код, виконуваний упродовж деякого часу
for(let i = 0; i <= 100; i += 1) {
	console.log(i);
}

const endTime = Date.now();
const elapsedTime = endTime - startTime;

console.log(`Elapsed time: ${elapsedTime} ms`);
```

```js
// Сколько дней до Нового года
const today = new Date();
const newYear = new Date(today.getFullYear() + 1, 0, 1);

const diffMs = newYear - today; // разница в миллисекундах
const daysLeft = Math.floor(diffMs / (1000 * 60 * 60 * 24));

console.log(`До Нового года осталось ${daysLeft} дней`);
```

---
## **Разница между `Date.now()` и `.getTime()`**

Оба метода возвращают **метку времени** (количество миллисекунд с 1 января 1970 UTC), но работают по-разному:

---
### 🔹 Date.now()

- **Статический метод** объекта `Date`
- Не требует создания экземпляра
- Возвращает метку времени для **текущего момента**

```js
console.log(Date.now()); 
// Например: 1724321234567
```

---
### **⚖️ Сравнение**

| **Метод**  | **Использование**                      | **Что возвращает**         |
| ---------- | -------------------------------------- | -------------------------- |
| Date.now() | Нужно узнать **текущее время**         | Метку времени «сейчас»     |
| .getTime() | Нужно узнать время **конкретной даты** | Метку времени объекта Date |
✅ Date.now() работает быстрее, так как не создаёт объект Date.
✅ new Date().getTime() делает то же самое, но медленнее.

---