## Описание

<font color="#409CFF">`fas:InfoCircle`</font> **JSON (JavaScript Object Notation)** – это текстовый формат обмена данными, который используется для хранения и передачи информации между сервером и клиентом.

<font color="#fcb97d">`ris:Question`</font> JSON – это как **таблица с данными в виде текста**, который легко читается и обрабатывается программами.
<font color="#fcb97d">`ris:Question`</font> JSON похож на **объект JavaScript**, но это просто **текст**.

```json
{
  "name": "Alice",
  "age": 25,
  "isStudent": false,
  "skills": ["JavaScript", "HTML", "CSS"],
  "address": {
    "city": "Berlin",
    "country": "Germany"
  }
}
```

> [!done] Здесь:
> - name – строка ("Alice")
> - age – число (25)
> - isStudent – булево значение (false)
> - skills – массив строк
> - address – вложенный объект

---
## Синтаксис

1. **Ключи всегда в двойных кавычках**:

```json
{ "name": "Alice" } ✅
{ name: "Alice" } ❌ Ошибка!
```

2. **Строки – в двойных кавычках**:

```json
{ "city": "Berlin" } ✅
{ "city": 'Berlin' } ❌ Ошибка!
```

3. **Нет комментариев** (в отличие от JavaScript):

```json
{
  "name": "Alice"
  // "age": 25 ❌ Ошибка!
}
```

4. **Последний элемент не должен содержать запятую**:

```json
{
  "name": "Alice",
  "age": 25
} ✅

{
  "name": "Alice",
  "age": 25,
} ❌ Ошибка!
```

---
## Как работать с JSON в JavaScript?
### Преобразование JSON в объект (JSON.parse)

JSON – это просто **текст**. Чтобы использовать его в коде, нужно **преобразовать в объект**.

```js
const jsonString = '{"name": "Alice", "age": 25}';

const user = JSON.parse(jsonString); // Преобразуем строку в объект

console.log(user.name); // Alice
console.log(user.age);  // 25
```

### Преобразование объекта в JSON (JSON.stringify)

Если у нас есть **объект**, и мы хотим передать его в сервер или сохранить, мы преобразуем его в JSON-строку.

```js
const user = { name: "Alice", age: 25 };

const jsonString = JSON.stringify(user); // Преобразуем объект в строку

console.log(jsonString); // '{"name":"Alice","age":25}'
```

> [!warning]
> Undefined и Функции игнорируются при переводе в формат JSON

---
## Пример №2



---
## ИТОГ:

  📌 
  
- 
