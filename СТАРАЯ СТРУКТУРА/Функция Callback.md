# **Функция Callback**

**Callback (коллбек)** — это функция, которая передаётся в другую функцию в качестве аргумента и вызывается позже, когда будет готов результат или наступит определённое событие.  

---

## 📌 Особенности
- Callback не вызывается сразу, а передаётся «на потом».  
- Используется для обработки результата или продолжения работы после завершения какой-то операции.  
- В JavaScript часто применяется в асинхронных задачах (таймеры, сетевые запросы, обработчики событий).  

---
## 🔹 Синтаксис

```js
function doSomething(value, callback) {
  console.log("Я сделал работу:", value);
  callback();
}

function afterWork() {
  console.log("Вызван callback!");
}

doSomething("🚀", afterWork);
```

![[Callback function ex.png]]

---
## **🔹 Примеры использования**
### **1. Таймер**

```js
setTimeout(() => {
  console.log("Прошло 2 секунды");
}, 2000);
```

Здесь коллбек — это функция () => { ... }, которая выполнится через 2 секунды.

---
### **2. Обработчик события**

```js
document.addEventListener("click", () => {
  console.log("Клик по документу!");
});
```

Коллбек сработает, когда произойдёт событие click.

---
### **3. Асинхронная операция (старый подход)**

```js
function getData(callback) {
  setTimeout(() => {
    callback("📦 Данные получены");
  }, 1000);
}

getData(result => console.log(result));
```

---
## Інлайн-колбеки (Inline callbacks)

Инлайн-колбек — это функция, которую мы передаём прямо в вызов, без вынесения её в отдельное имя.  

Часто записывается через [[Функция Анонимная]] или [[Функция Arrow Function]] функцию.

---
### Пример 1 — setTimeout

```js
setTimeout(function() {

  console.log("Через 1 секунду");

}, 1000);
```

---
### **Пример 2 — стрелочная функция**

```js
setTimeout(() => console.log("Через 1 секунду"), 1000);
```

---