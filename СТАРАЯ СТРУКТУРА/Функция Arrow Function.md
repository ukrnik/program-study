## **Функция Arrow Function (стрелочная функция)**

**Arrow Function** — это сокращённый синтаксис для объявления функций в JavaScript.  
Записывается с помощью стрелки  ` => `и чаще всего используется для **инлайн-функций** и [[Функция Callback]]. 

---
## 📌 Особенности
- Более короткая запись функций.  
- У стрелочных функций **нет своего `this`** — они используют `this` из внешнего окружения.  
- Не имеют собственного `arguments`.  
- Нельзя использовать как функцию-конструктор (`new`).  
- Удобны для простых выражений, особенно в перебирающих методах (`map`, `forEach`, `filter`).  

---
## 🔹 Синтаксис
### 1. Базовая форма

```js
const sum = (a, b) => {
  return a + b;
};
console.log(sum(2, 3)); // 5
```

### **2. Краткая форма (если тело — одно выражение)**

```js
const sum = (a, b) => a + b;
console.log(sum(2, 3)); // 5
```

### **3. Один аргумент (скобки можно опустить)**

```js
const square = x => x * x;
console.log(square(4)); // 16
```

### **4. Без аргументов**

```js
const sayHi = () => console.log("Привет!");
sayHi();
```

---
## **🔹 Примеры использования**
### **1. В методах массива**

```js
const numbers = [1, 2, 3, 4, 5];

const doubled = numbers.map(num => num * 2);
console.log(doubled); // [2, 4, 6, 8, 10]
```

### **2. В [[Метод .forEach(callback)]]**

```js
[10, 20, 30].forEach(value => console.log(value));
```

### **3. В таймерах**

```js
setTimeout(() => console.log("Прошло 2 секунды"), 2000);
```

---
## **⚖️ Отличие от обычной функции**

```js
// Обычная функция
function sayHi() {
  console.log(this);
}

// Стрелочная функция
const sayHiArrow = () => {
  console.log(this);
};

sayHi();       // this зависит от контекста вызова
sayHiArrow();  // this берётся из внешней области
```

---
## **📌 Итог**

Стрелочные функции — это компактная запись функций, которая упрощает работу с [[Функция Callback]] и улучшает читаемость кода.
Не подходят для случаев, где нужен свой this, arguments или функция-конструктор.