## Описание

Существует два основных события клавиатуры:

- `keydown` – событие, происходящее при нажатии клавиши
- `keyup` — происходящее событие, когда клавишу отпустили

В отличие от других событий события клавиатуры обрабатываются на документе, а не на конкретном элементе. Объекты событий клавиатуры исходят из базового класса [[Class KeyboardEvent|KeyboardEvent]].

```js
document.addEventListener("keydown", event => {
  console.log("Keydown: ", event);
});

document.addEventListener("keyup", event => {
  console.log("Keyup: ", event);
});
```

<font color="#fcb97d">`ris:Question`</font> События `keydown` и `keyup` срабатывают при нажатии любой клавиши, включая служебные (`Ctrl`, `ShiftAltEscape` и т.д.).

<font color="#fcb97d">`ris:Question`</font> На практике преимущественно обрабатывают только событие `keydown`, поскольку оно происходит быстрее `keyup` и пользователь раньше видит результат нажатия.

---
## Полезные свойства event в событиях клавиатуры

| Свойство       | Описание               | Пример                     |
| -------------- | ---------------------- | -------------------------- |
| event.key      | Название клавиши       | "a", "Enter", "Escape"     |
| event.code     | Код клавиши            | "KeyA", "Space", "ArrowUp" |
| event.shiftKey | true, если зажат Shift | true / false               |
| event.ctrlKey  | true, если зажат Ctrl  | true / false               |
| event.altKey   | true, если зажат Alt   | true / false               |

---
### Пример: проверяем, зажат ли Ctrl+S

```js
document.addEventListener("keydown", function(event) {
    if (event.ctrlKey && event.key === "s") {
        event.preventDefault(); // Остановим стандартное сохранение страницы
        console.log("Сочетание Ctrl + S нажато!");
    }
});
```

<font color="#fcb97d">`ris:Question`</font> Теперь, если нажать Ctrl + S, браузер **не откроет окно сохранения**, а в консоли появится сообщение.

---
## Разница между event.key и event.code

- `event.key` показывает букву, которая реально введена, с учётом раскладки.
- `event.code` показывает физическую клавишу, независимо от языка.
### Пример

- Если у вас **английская раскладка**, нажатие на Q:

```js
event.key  // "q"
event.code // "KeyQ"
```

- Если у вас **русская раскладка**, нажатие на Й:

```js
event.key  // "й"
event.code // "KeyQ"
```

<font color="#fcb97d">`ris:Question`</font> Это значит, что event.code удобен, если нужно отслеживать **конкретные клавиши**, а event.key — если важно учитывать язык ввода.

---
## Пример №1: Отмена стандартного поведения клавиш

Некоторые клавиши имеют **стандартные функции** в браузере (Ctrl + S, F5, Backspace). Если вам нужно заблокировать действие, используйте event.preventDefault().

<font color="#fcb97d">`ris:Question`</font> отключаем F5 (обновление страницы)

```js
document.addEventListener("keydown", function(event) {
    if (event.key === "F5") {
        event.preventDefault();
        console.log("Обновление страницы заблокировано!");
    }
});
```

---
## Пример №2: обработка стрелок

Допустим, у нас есть элемент, который нужно двигать с помощью стрелок (ArrowUp, ArrowDown, ArrowLeft, ArrowRight).

```js
const box = document.querySelector(".box");
let x = 0, y = 0;

document.addEventListener("keydown", function(event) {
    if (event.key === "ArrowRight") x += 10;
    if (event.key === "ArrowLeft") x -= 10;
    if (event.key === "ArrowUp") y -= 10;
    if (event.key === "ArrowDown") y += 10;
    
    box.style.transform = `translate(${x}px, ${y}px)`;
});
```

<font color="#fcb97d">`ris:Question`</font> Теперь, когда пользователь нажимает стрелки, квадрат будет двигаться по экрану.

---
## ИТОГ:

  📌 В JavaScript есть два события клавиатуры:

- `keydown` — при нажатии клавиши (используется чаще всего).
- `keyup` — когда клавиша отпущена.

📌 Основные свойства объекта события (event:

- `event.key` — какая клавиша нажата.
- `event.code` — физическая клавиша, независимо от языка.
- `event.shiftKey`, `event.ctrlKey`, `event.altKey` — проверка нажатых модификаторов.

📌 **Частые задачи**:

- Реакция на нажатие определённой клавиши.
- Запрет на стандартные действия (event.preventDefault()).
- Отслеживание комбинаций (Ctrl + S, Shift + A).
- Управление элементами на странице через клавиатуру.